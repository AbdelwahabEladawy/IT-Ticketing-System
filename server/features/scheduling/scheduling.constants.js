export const SCHEDULING_ALLOWED_ROLES = ['TECHNICIAN', 'IT_ADMIN', 'SUPER_ADMIN'];
export const SCHEDULING_TARGET_ROLES = ['TECHNICIAN', 'IT_ADMIN', 'SUPER_ADMIN'];

export const SCHEDULE_TYPE_ONE_TIME = 'ONE_TIME';
export const SCHEDULE_TYPE_YEARLY = 'YEARLY';
export const SCHEDULE_TYPES = [SCHEDULE_TYPE_ONE_TIME, SCHEDULE_TYPE_YEARLY];

export const DEFAULT_SCHEDULE_TIME_OF_DAY = '09:00';
export const DEFAULT_SCHEDULE_TIMEZONE = 'Africa/Cairo';

export const SCHEDULE_WORKER_INTERVAL_MS = Number(process.env.SCHEDULE_WORKER_INTERVAL_MS || 30000);
export const SCHEDULE_RETRY_DELAY_MS = Number(process.env.SCHEDULE_RETRY_DELAY_MS || 5 * 60 * 1000);

